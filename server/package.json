{
  "name": "web-socket-server",
  "version": "1.0.0",
  "description": "Server for WS example app",
  "main": "src/server.ts",
  "scripts": {
    "dev": "rm -rf ./build/server && ./node_modules/.bin/tsc && node ./build/server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Dmitrycmc",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "ws": "^8.5.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/ws": "^8.5.3",
    "typescript": "^4.6.2"
  }
}
