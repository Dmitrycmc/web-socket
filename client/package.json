{
  "name": "web-socket-client",
  "version": "1.0.0",
  "description": "Client for WS example app",
  "main": "index.js",
  "scripts": {
    "compile": "rm -rf ../server/build/client && ./node_modules/.bin/tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dmitrycmc",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.6.2"
  }
}
